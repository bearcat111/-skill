# 工作流细化清单与提问话术

本文件是 SKILL.md 各阶段的细化操作清单，供执行时对照。所有与用户的沟通使用简体中文。

---

## 阶段 1 · 设备清单采集清单

逐项采集，用下表汇总后回显确认：

| 序号 | 角色 | 品牌 | 型号 | 数量 | 冗余/堆叠方式 |
|------|------|------|------|------|---------------|
| 1 | 边界防火墙 | 华为 | USG6000 | 2 | 热备(HRP) |
| 2 | 外网出口路由器 | 华三 | MSR | 2 | 双出口/主备 |
| 3 | 三层核心交换机 | 华为 | S5735 | 2 | 堆叠(iStack) |
| 4 | 二层接入交换机 | 思科 | C2960 | 2 | 独立 |
| 5 | DMZ Web 服务器 | — | — | 1 | 单机 |

话术示例："请告诉我这个项目里有哪些设备？按'角色—品牌—型号—数量'告诉我，例如'边界防火墙用华为 USG6000 两台做热备'。上面只是模板，请按你的真实需求提供。"

---

## 阶段 2 · 互联与逻辑设计采集清单

收集后逐项记录，未提供的留空：

- [ ] 管理网段 / 管理 VLAN
- [ ] 各设备**管理 IP**
- [ ] **互联链路**：A 设备 接口X ↔ B 设备 接口Y（含链路 IP、掩码）
- [ ] **VLAN 规划**：VLAN ID、名称、所属设备、放行网段
- [ ] **路由协议参数**：协议类型、进程号/AS、区域、router-id
- [ ] **冗余协议**：VRRP 组、虚拟 IP、优先级
- [ ] **链路聚合**：LACP 组、成员接口、模式
- [ ] **安全策略**：ACL 编号与用途、NAT 方式、防火墙安全域/策略
- [ ] **认证与监控**：AAA/RADIUS、SNMP 团体字/目标、Syslog 目标、BFD/NQA

---

## 阶段 3 · 关键决策提问模板（必须暂停）

用 `AskUserQuestion` 抛出，选项含"其他"。若用户已在阶段 1/2 明确给出某项，可跳过。

**问题 1 — 路由协议**
- 选项：OSPF（推荐，园区网通用）/ IS-IS / 静态路由为主 / BGP（多出口或运营商互联）
- 追问（若选 OSPF）：进程号、区域划分（建议核心 area 0，接入非 0）、router-id 来源（Loopback）。

**问题 2 — 防火墙高可用**
- 选项：主备热备（HRP+VRRP，推荐）/ 负载分担（双主）/ 单机（无冗余）

**问题 3 — NAT 策略**
- 选项：出方向 PAT（内网共享公网 IP，推荐）/ DMZ 静态一对一映射 / 双向 NAT / 暂不需要 NAT

**问题 4 — 链路聚合与虚拟化**
- 选项：需要 LACP 链路聚合 / 需要设备堆叠（iStack/IRF/vPC）/ 两者都要 / 都不需要

**问题 5 — 管理与认证**
- 选项：启用 AAA+本地账号 / 仅本地账号 / 接入 RADIUS/TACACS+ ；并确认管理网段。

**问题 6 — 监控与故障检测**
- 选项：启用 SNMP+Syslog / 启用 BFD/NQA 链路检测 / 全部启用 / 暂不配置

> 任何一项用户未回答前，不得进入阶段 4。可一次问 1–4 个，分轮完成。

---

## 阶段 4 · 草案生成与自定规则

AI 自动补全的非必要项及默认规则：
- **VLAN ID**：从 10 起按业务递增（管理 10、业务 20、语音 30、DMZ 40…）， trunk 放行所需列表。
- **互联接口**：按设备上联口顺序取（如 GE0/0/1、GE1/0/1），下联对应。
- **链路 IP**：互联网段用 /30，管理用 /24，业务按规模。
- **router-id / Loopback0**：取 `x.x.x.x`（如核心 1.1.1.1、2.2.2.2）。
- **LACP 组号**、**VRRP 组号**、**OSPF 进程号**默认从 1 起。

草案回显后务必说明："以上灰色/未指定项由我代为规划，回复'没问题'即按此执行。" 收到确认后锁定基线。

---

## 阶段 5 · 可行性分析检查表（逐项打勾）

| 类别 | 检查点 | 常见问题 | 解决方案 |
|------|--------|----------|----------|
| IP | 冲突/掩码/互联余量 | 同网段重复、/24 当 /30 用 | 重划网段、改用 /30 |
| 路由 | 黑洞/环路/次优 | 缺默认路由、防火墙拦 OSPF | 下发 default-info、放行协议 |
| 二层 | 根桥/Trunk 一致性 | 根桥未指定导致震荡 | 手动 `root primary`、收敛 allow 列表 |
| 高可用 | 脑裂/抢占 | VRRP 优先级相同、BFD 时长不匹配 | 设优先级差、统一检测时长 |
| 安全 | ACL 方向/DMZ 暴露 | 方向反、通配符错 | 复核 in/out、最小放行 |
| 厂商能力 | 型号支持度 | C2960 不能跑 OSPF | 改由三层交换机承担路由 |
| 可部署 | CLI 语法 | 命令与厂商不符 | 对照 references/*.md 修正 |

将"问题→影响→方案"报用户，确认后再进阶段 6。

---

## 阶段 6/7 · 输出清单自检

- [ ] Word 文档含：概览、拓扑图、台账表、逐模块详述
- [ ] 每台设备均有独立 `主机名_角色_v1.txt`
- [ ] 文件名主机名与文件内 `hostname` 一致
- [ ] 配置含保存命令（华为 `save`、思科 `write memory`、华三 `save`）
- [ ] 所有命令已对照厂商参考文件核对语法
