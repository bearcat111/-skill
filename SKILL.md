---
name: network-deploy
description: 网络项目部署技能。当用户需要规划并落地一个企业网/园区网项目时使用，典型场景为经典三层架构（接入层/汇聚层/核心层）或包含防火墙、出口路由器、二/三层交换机、服务器（DMZ）的网络。技能驱动一套强交互式工作流：阶段1-5为"咨询与确认"（必须不断与用户对话、列选项、等明确确认，绝不在这些阶段擅自决定关键项，也不提前写配置/文档），阶段6-7才真正产出；支持在记忆中缺乏某设备/型号命令时上网查证思科/华为/华三官方文档。最终交付一份 Word 项目文档（拓扑图+台账表+详述）与一组按"主机名_角色_v1.txt"命名的逐设备配置文件，使用者照配置文件输入命令即可部署。
agent_created: true
---

# 网络项目部署（Network Deploy）

## 概述

本技能将"网络项目规划 + 落地部署"固化为一套可复用的强交互式工作流。它以经典三层架构（接入层 / 汇聚层 / 核心层）为范式，但可泛化到任意包含防火墙、出口路由器、二/三层交换机、服务器区（DMZ）的网络。技能的最终交付物是：

1. 一份 **Word 项目文档**（拓扑图 + 台账表 + 逐模块文字说明）；
2. 一组 **逐设备配置文件**，按 `主机名_角色_v1.txt` 命名，使用者照着敲命令即可部署。

**最重要的定位区分：**
- **阶段 1–5 = 咨询与确认阶段**：这一阶段的唯一产出是"澄清 + 共识"。必须通过与用户的多轮对话，把每一个关键决策都变成"用户明确点头或改选"的状态。**严禁在阶段 1–5 内擅自写入任何配置文件或文档**（那是阶段 6/7 的产出）。
- **阶段 6–7 = 产出阶段**：用户已对设计有清晰认知并确认后，才真正生成 Word 文档与逐设备配置。

当某设备/型号的命令行不在本技能记忆（`references/*.md`）中、或担心新机型命令有细微差异时，技能会**主动上网查证**厂商官方文档，确保配置命令准确可落地（见"Web 命令查证能力"一节）。

## 何时使用

出现以下任意信号即触发本技能：

- "帮我规划一个三层架构网络""部署一个园区网/企业网""做一个带防火墙和 DMZ 的网络项目"；
- 用户开始列出设备（如"思科 c2960 两台、华为 s5735 两台、USG6000 两台做热备、华三路由器两台"）并提及品牌/型号/数量；
- 用户要求输出网络拓扑、VLAN 规划、IP 规划、设备配置命令或部署文档。

## 运行环境与前置准备

- 生成 Word 文档依赖 `python-docx`。在**本运行环境提供的 Python 虚拟环境**中安装（不要污染用户全局 Python）：
  - 路径因运行环境而异，执行时请把 `<PYTHON>` 替换为本环境实际的 Python 解释器路径，`<VENV_DIR>` 替换为虚拟环境目录：
    ```
    <PYTHON> -m venv <VENV_DIR>
    <VENV_DIR>/Scripts/pip.exe install python-docx   # Windows
    # <VENV_DIR>/bin/pip install python-docx         # Linux / macOS
    ```
  - 若虚拟环境已具备 `python-docx`，可跳过安装；脚本运行时使用对应虚拟环境解释器。
- 为本项目建立独立工作目录，建议：`网络项目/部署_<项目名>/`，文档与配置文件均落在此目录下。

## 对话式确认纪律（最高优先级，务必遵守）

> 这是本技能最关键的约束，覆盖阶段 1–5 的全部行为。

1. **阶段 1–5 是咨询，不是产出。** 在此区间内，你只能提问、列清单、给建议、做草案；**不得**生成配置文件、不得生成 Word 文档、不得把任何设计"偷偷定死"。
2. **每个阶段结束都必须先拿到用户明确确认，才能进下一阶段。** 阶段 3、4、5 是硬关卡，缺一不可。
3. **即使用户说"你自行决定 / 你看着办 / 随便 / 都行"，也绝不豁免确认。** 此时你的正确做法是：把该决策点的**选项清单**（含你推荐的"推荐项"）列出来，让用户逐一点头或改选。"自行决定"对用户只表示"采纳你给的推荐项"，**不等于"不用问"**。
   - ❌ 错误示范（曾经犯过）：用户没提 NAT，你就在阶段 6/7 直接"按纯路由实现、未做 NAT"，事后再补一句"若要出公网补 PAT 即可"。——这是**不允许**的。
   - ✅ 正确做法：在阶段 3/4 把"是否做 NAT"作为一个明确决策项抛给用户，给出"① 做 PAT 出公网（推荐）/ ② 仅 DMZ 静态映射 / ③ 暂不需要 NAT（纯模拟网）"等选项，等用户选完才进入下一步。
4. **所有关键决策项（路由协议、NAT、防火墙 HA、链路聚合/虚拟化、AAA、监控、DHCP 策略、VLAN/网段规划等）都必须出现在决策清单里**，逐项确认；未确认不得越过关卡。具体端口号、VLAN ID 数值、IP 末位等非关键信息可由你合理分配，但必须列在草案中供审查。
5. **用 `AskUserQuestion` 工具把选项抛给用户**（一次 1–4 个问题，含"其他"自由输入）。对"方向性"问题务必给推荐项，并清楚写出每个选项的后果。
6. 只有当你确认：**所有关键项已获用户明确表态、你对整个项目有完全清晰的认知、且不存在未解决的歧义**，才允许进入阶段 6。

## Web 命令查证能力（确保命令准确，尤其在记忆缺失/新机型时）

在生成配置前（阶段 5 可行性核查与阶段 7 写配置时各做一次"命令可得性检查"），若遇到以下任一情况，**必须上网查证**，不要凭记忆硬写：

- 设备型号不在 `references/cisco_cmds.md` / `huawei_cmds.md` / `h3c_cmds.md` 覆盖范围内；
- 该型号是新机型，担心命令语法有细微变化（如 VRP 版本差异、Cisco IOS-XE 与经典 IOS 差异、H3C Comware V7/V9 差异）；
- 需要某个高级/少见特性的配置样例（如特定防火墙的 HRP、特定交换机的 M-LAG、802.1x 的 RADIUS 集成等）。

查证流程（详见 `references/web_research.md`）：
1. 用 `WebSearch` 检索厂商官方文档/配置指南，关键词模板见 `references/web_research.md`（Cisco / 华为 / 华三 各自的官方入口与检索式）。
2. 用 `WebFetch` 打开最相关的官方页面，提取**准确命令片段**与配置上下文。
3. 把查证到的正确命令带回设计，并在配置文件中据此书写。**若官方站点无法访问或内容需登录**，退而检索厂商社区/技术博客，并在文件注释中标注"命令未经官方页面二次确认，建议部署前在真机/模拟器核对"。
4. 若同一型号/特性被反复用到，把验证过的命令片段补充进 `references/*.md`（通过 SkillManage 累积，方便下次复用），并在本轮向用户说明"已联网核对 X 型号命令"。

> 注意：上网查证是为了"命令准确"，它**不能替代阶段 3/4/5 的确认**。即便命令已查证，关键设计决策仍须用户拍板。

## 工作流总览（七阶段）

```
阶段1 采集设备清单       →  阶段2 采集互联与逻辑设计
   →  阶段3 【关键决策·必须暂停询问·列选项等确认】
   →  阶段4 【完整草案·列选项等确认】
   →  阶段5 【可行性分析·列问题/方案·列选项等确认】
   →  阶段6 生成 Word 项目文档   →  阶段7 生成逐设备配置文件
   →  阶段7.5 交付验收清单（references/verify.md）
   →  【增强轮次·可选】询问 8 安全加固 / 9 参考补全，若选则生成 v2 文档与 v2 配置
```

阶段 1–5 只做"澄清 + 共识"，阶段 6–7 才做"产出"。**阶段 3、4、5 每个都是硬关卡：未拿到用户明确确认不得越过。**

---

## 阶段 1 · 采集设备清单

向用户索取每台设备的：**角色、品牌、型号、数量**。先用澄清式提问收集，再汇总成清单表格回显给用户确认。

常见角色分类（可按实际增减）：
- 边界防火墙（如华为 USG6000、Cisco ASA/Firepower）
- 外网出口设备（路由器，如华三 MSR、Cisco ISR）
- 核心/三层交换机（如华为 S5735/S7700/CE 系列、Cisco Catalyst 9k）
- 汇聚/三层交换机
- 二层接入交换机（如 Cisco C2960、华为 S2700）
- 服务器/业务主机（DMZ 区 Web 服务器等）

> 提示：记录每个角色的"台数"，并约定主备/堆叠关系（如"两台做热备""两台堆叠"）。若用户给出的型号你不确定命令，记下型号，留到阶段 5/7 前做 Web 查证。

**结束条件**：用户确认设备清单（角色/品牌/型号/数量/冗余关系）无误，才进阶段 2。

## 阶段 2 · 采集互联与逻辑设计

逐项向用户收集（用户给多少记多少，未给的留空，交给阶段 3/4 处理）：
- **IP 规划**：每台设备管理 IP、互联链路 IP、业务网段、管理网段；
- **物理互联**：哪台设备的哪个接口，连到哪台设备的哪个接口（接入层↔汇聚层↔核心层↔防火墙↔出口）；
- **VLAN**：创建哪些 VLAN、ID、名称、归属接口、放行网段；
- **协议**：运行什么路由协议、交换协议（STP/RSTP/MSTP）、冗余协议（VRRP）、链路聚合（LACP）、虚拟化（iStack/IRF/vPC）；
- **策略**：ACL、NAT、安全策略、AAA 认证、SNMP/Syslog 监控、BFD/NQA 故障检测等。

**结束条件**：用户确认已把所有"自己心里有数"的信息都给了你，剩余空白项交给后续阶段处理，才进阶段 3。

## 阶段 3 · 关键决策【必须暂停询问用户 · 列选项等确认】

**这是本技能最关键的约束之一。** 当用户未明确给出下列"方向性"决策时，**必须停下来逐一询问**，直到用户对网络项目有了清晰认知后才能继续。即使用户说"你决定"，也必须列出含推荐项的选项清单让用户确认或改选（见"对话式确认纪律"第 3 条）。使用 `AskUserQuestion` 工具提供选项（含"其他"自由输入），一次问清 1–4 个问题。

必须确认的关键项（如用户已给则跳过对应项）：
1. **路由协议选型**：OSPF / IS-IS / 静态路由 / BGP（含进程号、区域划分、router-id）；
2. **防火墙高可用**：热备（主备 VRRP/HRP）/ 负载分担 / 单机；
3. **NAT 策略**：出方向 PAT（地址复用）/ DMZ 静态一对一映射 / 双向 NAT / **暂不需要 NAT（纯模拟网）** —— 每一项都要明确问，不得默认跳过；
4. **链路聚合 / 设备虚拟化**：是否需要 LACP、是否堆叠（iStack/IRF/vPC），以及堆叠域/域编号；
5. **管理 & 认证**：管理网段、是否启用 AAA/RADIUS、本地账号体系；
6. **监控与故障检测**：SNMP/Syslog 目标、是否启用 BFD/NQA/VRRP 跟踪；
7. **DHCP 策略**：由哪台设备做 DHCP 服务器、地址池如何划分、是否做中继（relay）；
8. **二层安全**：是否启用端口安全、DHCP Snooping、DAI、802.1x、MAC 黑名单等。

非必要信息（具体端口号、VLAN ID 数值、IP 末位、接口命名）**不要**在此追问，交由阶段 4 自动分配并在草案中列出。

**结束条件**：上述关键项全部获得用户明确表态（含"采纳推荐项"），才进阶段 4。

## 阶段 4 · 完整草案清单供审查【必须暂停询问用户 · 列选项等确认】

基于阶段 1–3，AI 主动补全所有未指定的非必要细节，生成一份**完整草案**回显给用户：
- 自动分配 VLAN ID（如 10/20/30…）、接口（如 GE0/0/1）、IP 末位、链路聚合组号、router-id（常用 Loopback0 地址）；
- 画出**拓扑草案**（ASCII 框图，参考 `references/topology_examples.md`）；
- 列出设备台账初稿、协议与策略规划、各关键决策的落地点（例如"NAT：在 R1 做出方向 PAT，匹配内网 10.0.0.0/8"）。

明确告知用户："以下非必要项由我代为规划，**关键决策均已按你上一阶段的确认落地**；请逐项审查，如有需要调整的地方请指出，确认'没问题'后我才进入可行性分析与正式产出。" 待用户明确回复"没问题/确认"后，将草案锁定为正式设计基线，进入阶段 5。

**结束条件**：用户明确确认草案无误（或提出调整后你已修订并再次获确认），才进阶段 5。

## 阶段 5 · 可行性分析【必须暂停询问用户 · 列问题/方案等确认】

在锁定设计后、动笔写文档前，AI 必须**主动审查整个方案**，逐项排查潜在问题并给出可执行方案，向用户报告。至少覆盖：

- **IP/子网**：是否存在地址冲突、掩码是否合理、互联网段是否够用、是否出现 31 位/30 位误用；
- **路由**：是否存在黑洞/次优路径/环路、OSPF 区域设计是否合理、是否需下发默认路由、防火墙是否放行 OSPF/BGP；
- **二层**：STP 根桥是否手动指定、是否存在双工/VLAN 透传不一致、Trunk 允许列表是否收敛；
- **高可用**：堆叠/热备是否会有脑裂、VRRP 优先级与抢占是否合理、BFD 检测时长是否匹配；
- **安全**：ACL 方向/通配符是否正确、DMZ 是否暴露过度、管理平面是否隔离、是否存在明文协议；
- **厂商能力**：所填型号是否支持规划特性（例：C2960 为纯二层，不能做三层路由/OSPF；部分特性需授权 License）——若型号不支持，必须提示并建议替代；
- **可部署性 / 命令可得性**：配置命令是否与厂商 CLI 语法一致；**对记忆中没有或新机型的命令，是否已经过 Web 查证**（见"Web 命令查证能力"）。

将"问题 → 影响 → 解决方案"逐条列出，**对每条方案也给出选项让用户确认/改选**（例如"根桥方案：A 手动指定 DSW1 为 MST 总根(推荐) / B 保持自动选举"）。在报告末尾明确问用户："以上风险处理方案是否都按我建议的来？还是某项要改？" **直至 AI 对整个项目有完全清晰认知、且确认所有方案已获用户认可，才进入阶段 6**。

**结束条件**：用户对可行性分析的所有风险处理方案明确确认（或改选后再次确认），你已无未决歧义，才进阶段 6。

> **设计基线持久化（阶段 5 结束动作）**：可行性分析全部确认后，把锁定设计写入项目目录 `设计基线.md`（设备清单、VLAN/编址、互联、协议、关键决策、风险方案）。这是阶段 6/7 与后续 v2/v3 增强的唯一依据，也是会话中断后的恢复点。
> **跨厂商聚合/堆叠硬性规则（踩坑沉淀）**：仅**同厂商**设备间可做 LACP/堆叠（iStack/IRF/vPC/M-LAG）；**跨厂商**上联一律用**静态 Trunk**，禁止跨厂商 LACP（私有协议不互通）。可行性分析必须包含此项检查，违反则改为静态 Trunk 并报用户。

## 阶段 6 · 生成 Word 项目文档

调用 `scripts/gen_docx.py` 生成文档。先构造一个 JSON 项目模型（结构见脚本头部说明），写入 `项目模型.json` 后运行脚本。文档须包含：

1. **项目概览**：背景、目标、三层架构说明；
2. **拓扑图**：用 ASCII 框线/线段抽象描绘（接入↔汇聚↔核心↔防火墙↔出口↔DMZ），参考 `references/topology_examples.md`；
3. **台账表（多张表格）**：设备命名(hostname)、品牌型号、角色、管理 IP、互联接口、归属 VLAN、运行协议、认证账号/密码、备注；
4. **逐模块详述**：用文字解释设计意图与关键点，例如：
   - "ACL 2000 用于出方向 NAT 地址池匹配，仅放行内网网段 10.x/172.x/192.168.x"；
   - "DSW1 的 Loopback0 = 1.1.1.1，作为 OSPF router-id 与远程管理源"；
   - "在核心与汇聚之间为路由优选做了 OSPF cost 调整 / 做了 VRRP 主备切换" 等；
5. **关键决策记录**：把阶段 3/4/5 中用户确认的所有关键决策（含 NAT 是否做、路由协议、HA 方式等）汇总成表，作为项目基线留档。

> 安全提示：文档含账号密码等敏感信息，生成后提醒用户妥善保管，避免明文外发。

## 阶段 7 · 生成逐设备配置文件

为**每一台**网络设备生成独立配置文件，命名规则：

```
<主机名>_<角色>_v1.txt
```
- 主机名取设备 `hostname` 配置值（如 `hostname DSW1` → 主机名 `DSW1`）；
- 角色取中文简称：出口设备 / 边界防火墙 / 三层交换机 / 二层交换机 / 服务器 等；
- 示例：`AR1_出口设备_v1.txt`、`FW1_边界防火墙_v1.txt`、`DSW1_三层交换机_v1.txt`、`ASW1_二层交换机_v1.txt`；
- 若配置文件生成后，用户还有要补充的内容且需要修改配置，此时保持原有的配置文件不变，再创建一份新的配置文件，命名示例 `AR1_出口设备_v2.txt`，新文件的版本号v1、v2、v3……以此类推。

**配置命令必须与厂商 CLI 严格一致**，按品牌加载对应参考，并对记忆外/新机型命令做 Web 查证：

- 思科 → `references/cisco_cmds.md`（必要时 Web 查证 Cisco 官方 Configuration Guide）
- 华为 → `references/huawei_cmds.md`（必要时 Web 查证华为企业业务文档）
- 华三 → `references/h3c_cmds.md`（必要时 Web 查证 H3C 官方命令参考）

**注释符按厂商分派（必须严格遵守）**：生成每台配置前，先确定其品牌，再统一使用该品牌的注释符——
- **思科（Cisco IOS/IOS-XE）**：用 `!` 作注释/分段符；
- **华为（VRP）/ 华三（Comware）**：用 `#` 作注释/分段符（这同时也是官方配置文件的段落分隔符）。

> ⚠️ **切勿在华为/华三配置里使用 `!`**：VRP/Comware 不把 `!` 当作注释，真机或模拟器会报 `Error: Unrecognized command`，可能中断粘贴部署。写完整台配置后，**必须**运行 `scripts/normalize_comments.py --vendor <cisco|huawei|h3c> --in <文件> --in-place` 把注释符强制归一化到该厂商标准（思科=`!`、华为/华三=`#`）。引用 `references/*.md` 命令时，注释符同样以目标厂商为准，勿照搬示例注释字符到错误厂商。

每台文件须包含完整可执行的配置块：主机名、管理 IP/接口、VLAN、Trunk/Access、LACP、OSPF/IS-IS/静态、VRRP、ACL、NAT、AAA、SNMP/Syslog、BFD/NQA、保存命令（`write memory` / `save`）等，并以"逐行可执行"为标准（使用者直接粘贴即可）。

> **阶段 7.5 验收清单**：阶段 7 所有配置写完后，把 `references/verify.md` 的验收清单交给用户，提示"导入 eve-ng / 真机后逐项验证"；连通性、OSPF 邻居、VRRP 主备、DHCP、NAT、802.1x、STP、聚合、BFD、AAA/SSH 全部 ✅ 视为部署成功（详见 workflow.md 阶段 7.5）。

## 阶段 7 之后 · 增强轮次（8 / 9，用户可选）

阶段 7（含 7.5 验收）完成后，用 `AskUserQuestion` 询问用户是否需要下列增强（可多选）：
- **8 安全加固基线**：对全网设备叠加 `references/hardening.md` 的加固项（禁用 Telnet/HTTP、仅 SSH v2、管理平面隔离、NTP、Syslog、控制平面保护等）。
- **9 参考库小补全应用**（已固化进技能参考文件）：拓扑范例 C（纯交换园区）、glossary 增补（MLAG/iStack/IRF、VXLAN、QoS、NTP、route-map/PBR）、MST 实例与 workflow 一致化。

若用户选择任一增强，进入**增强轮次**：以 `设计基线.md` 为依据，在原设计上叠加 8/9 内容，重新生成**版本化交付物**——文档 `项目设计文档_v2.docx`、每台配置 `<主机名>_<角色>_v2.txt`（v2、v3… 依增强轮次递增），旧版本保留不覆盖；文档新增「安全加固说明 / 参考补全说明」章节并说明与 v1 差异；变更部分同样跑 `normalize_comments.py`；最后重新走一次阶段 7.5 验收（仅验证受影响项）。

## 输出目录结构

```
网络项目/部署_<项目名>/
├── 项目设计文档.docx        # 阶段6 生成
├── 项目模型.json            # 阶段6 中间产物
├── 配置/
│   ├── AR1_出口设备_v1.txt
│   ├── FW1_边界防火墙_v1.txt
│   ├── DSW1_三层交换机_v1.txt
│   ├── ASW1_二层交换机_v1.txt
│   └── ...
└── 拓扑图.png（可选，若用户需要可视化另绘）
```

## 重要纪律（务必遵守）

- **阶段 1–5 只做咨询与确认，绝不提前产出配置或文档。**
- **阶段 3/4/5 的确认关卡不可豁免**：即使用户说"你决定"，也必须列含推荐项的选项清单让用户确认或改选。
- **关键方向性决策（路由协议、NAT、防火墙 HA、链路聚合/虚拟化、AAA、监控、DHCP、二层安全）全部必须显式确认**，不得静默默认。
- **记忆外或新机型命令必须 Web 查证**，确保语法准确可落地。
- **阶段 5 可行性分析是强制关卡**，发现问题必须上报并给方案（且方案也要用户确认），不得带病进入文档/配置生成。
- **配置命令务必对照厂商参考文件 + Web 查证结果**，杜绝语法错误导致部署失败。
- 全程用简体中文与用户沟通；文档与配置中的注释可用英文/中文，保持清晰一致。

## 资源索引

- `scripts/gen_docx.py` — Word 文档生成器（读取 JSON 项目模型）。
- `scripts/normalize_comments.py` — 按厂商归一化配置文件注释符（思科=`!`、华为/华三=`#`），防止写配置时串味导致粘贴报错。
- `references/workflow.md` — 各阶段细化清单、决策清单模板与提问话术。
- `references/web_research.md` — 上网查证思科/华为/华三官方配置命令的方法与检索式。
- `references/topology_examples.md` — 三层架构 ASCII 拓扑范例。
- `references/cisco_cmds.md` / `huawei_cmds.md` / `h3c_cmds.md` — 各厂商配置命令速查（记忆内命令；记忆外须 Web 查证）。
- `references/glossary.md` — ACL、Loopback、VRRP、OSPF cost、虚拟化、VXLAN、QoS、NTP、PBR 等概念的解释话术，供文档详述章节复用。
- `references/verify.md` — 阶段 7.5 部署验收清单（连通性/OSPF/VRRP/DHCP/NAT/802.1x/STP/聚合/BFD/AAA 验证项与查看命令）。
- `references/hardening.md` — 增强轮次·安全加固基线（禁用明文协议、仅 SSH v2、管理平面隔离、NTP/Syslog/控制平面保护）。
- `references/model_schema.json` — 项目模型 JSON 模板（含 `key_decisions` 字段），供构造阶段 6 模型时参考。
