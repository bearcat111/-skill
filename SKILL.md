---
name: network-deploy
description: 网络项目部署技能。当用户需要规划并落地一个企业网/园区网项目时使用，典型场景为经典三层架构（接入层/汇聚层/核心层）或包含防火墙、出口路由器、二/三层交换机、服务器（DMZ）的网络。技能驱动一套交互式工作流：采集设备清单（品牌/型号/数量）、采集互联与逻辑设计（IP/接口/VLAN/协议/策略）、在关键决策点暂停向用户确认、自动补全非必要细节、做可行性分析、生成 Word 项目文档（拓扑图+台账表+详述）以及按"主机名_角色_v1.txt"命名的逐设备配置文件，使使用者只需照配置文件输入命令即可完成部署。
agent_created: true
---

# 网络项目部署（Network Deploy）

## 概述

本技能将"网络项目规划 + 落地部署"固化为一套可复用的交互式工作流。它以经典三层架构（接入层 / 汇聚层 / 核心层）为范式，但可泛化到任意包含防火墙、出口路由器、二/三层交换机、服务器区（DMZ）的网络。技能的最终交付物是：

1. 一份 **Word 项目文档**（拓扑图 + 台账表 + 逐模块文字说明）；
2. 一组 **逐设备配置文件**，按 `主机名_角色_v1.txt` 命名，使用者照着敲命令即可部署。

整个流程强调：**该问的必须问，能定的自己定**。对于路由协议选型、防火墙高可用方式、NAT 策略、是否做链路聚合/虚拟化等"方向性"决策，必须暂停向用户确认；对于具体端口号、VLAN ID、IP 末位等非关键信息，AI 可自行合理分配并在清单中列出供用户审查。

## 何时使用

出现以下任意信号即触发本技能：

- "帮我规划一个三层架构网络""部署一个园区网/企业网""做一个带防火墙和 DMZ 的网络项目"；
- 用户开始列出设备（如"思科 c2960 两台、华为 s5735 两台、USG6000 两台做热备、华三路由器两台"）并提及品牌/型号/数量；
- 用户要求输出网络拓扑、VLAN 规划、IP 规划、设备配置命令或部署文档。

## 运行环境与前置准备

- 生成 Word 文档依赖 `python-docx`。首次使用前，在**托管 Python 虚拟环境**中安装（不要污染用户环境）：
  ```
  C:/Users/linxun/.workbuddy/binaries/python/versions/3.13.12/python.exe -m venv C:/Users/linxun/.workbuddy/binaries/python/envs/default
  C:/Users/linxun/.workbuddy/binaries/python/envs/default/Scripts/pip.exe install python-docx
  ```
  若已存在虚拟环境，仅执行 `pip install python-docx` 即可。所有脚本均用该虚拟环境解释器运行。
- 为本项目建立独立工作目录，建议：`网络项目/部署_<项目名>/`，文档与配置文件均落在此目录下。

## 工作流总览（七阶段）

```
阶段1 采集设备清单  →  阶段2 采集互联与逻辑设计  →  阶段3 【关键决策·必须暂停询问】
   →  阶段4 生成草案供审查（非必要信息AI自定）  →  阶段5 可行性分析（风险+方案）
   →  阶段6 生成 Word 项目文档  →  阶段7 生成逐设备配置文件
```

每个阶段完成后再进入下一阶段。**阶段 3 与阶段 5 是硬关卡**：未拿到用户明确确认不得越过。

---

## 阶段 1 · 采集设备清单

向用户索取每台设备的：**角色、品牌、型号、数量**。先用澄清式提问收集，再汇总成清单表格回显给用户确认。

常见角色分类（可按实际增减）：
- 边界防火墙（如华为 USG6000、Cisco ASA/Firepower）
- 外网出口设备（路由器，如华三 MSR、Cisco ISR）
- 核心/三层交换机（如华为 S5735/S7700、Cisco Catalyst 9k）
- 汇聚/三层交换机
- 二层接入交换机（如 Cisco C2960、华为 S2700）
- 服务器/业务主机（DMZ 区 Web 服务器等）

> 提示：记录每个角色的"台数"，并约定主备/堆叠关系（如"两台做热备""两台堆叠"）。

## 阶段 2 · 采集互联与逻辑设计

逐项向用户收集（用户给多少记多少，未给的留空，交给阶段 3/4 处理）：
- **IP 规划**：每台设备管理 IP、互联链路 IP、业务网段、管理网段；
- **物理互联**：哪台设备的哪个接口，连到哪台设备的哪个接口（接入层↔汇聚层↔核心层↔防火墙↔出口）；
- **VLAN**：创建哪些 VLAN、ID、名称、归属接口、放行网段；
- **协议**：运行什么路由协议、交换协议（STP/RSTP/MSTP）、冗余协议（VRRP）、链路聚合（LACP）、虚拟化（iStack/IRF/vPC）；
- **策略**：ACL、NAT、安全策略、AAA 认证、SNMP/Syslog 监控、BFD/NQA 故障检测等。

## 阶段 3 · 关键决策【必须暂停询问用户】

**这是本技能最重要的约束。** 当用户未明确给出下列"方向性"决策时，**必须停下来逐一询问**，直到用户对网络项目有了清晰认知后才能继续。不得自行臆断。使用 `AskUserQuestion` 工具提供选项（含"其他"自由输入），一次问清 1–4 个问题。

必须确认的关键项（如用户已给则跳过对应项）：
1. **路由协议选型**：OSPF / IS-IS / 静态路由 / BGP（含进程号、区域划分、router-id）；
2. **防火墙高可用**：热备（主备 VRRP/HRP）/ 负载分担 / 单机；
3. **NAT 策略**：出方向 PAT（地址复用）、DMZ 静态映射、是否做双向 NAT；
4. **链路聚合 / 设备虚拟化**：是否需要 LACP、是否堆叠（iStack/IRF/vPC），以及堆叠域/域编号；
5. **管理 & 认证**：管理网段、是否启用 AAA/RADIUS、本地账号体系；
6. **监控与故障检测**：SNMP/Syslog 目标、是否启用 BFD/NQA/VRRP 跟踪。

非必要信息（具体端口号、VLAN ID 数值、IP 末位、接口命名）**不要**在此追问，交由阶段 4 自动分配。

## 阶段 4 · 生成草案清单供审查（AI 自定非必要项）

基于阶段 1–3，AI 主动补全所有未指定的非必要细节，生成一份**完整草案**回显给用户：
- 自动分配 VLAN ID（如 10/20/30…）、接口（如 GE0/0/1）、IP 末位、链路聚合组号、router-id（常用 Loopback0 地址）；
- 画出**拓扑草案**（ASCII 框图，参考 `references/topology_examples.md`）；
- 列出设备台账初稿、协议与策略规划。

明确告知用户："以下非必要项由我代为规划，如无异议回复'没问题'即按此执行；如需调整请指出。" 待用户确认"没问题"后，将草案锁定为正式设计基线，进入阶段 5。

## 阶段 5 · 可行性分析（风险自查 + 方案）

在锁定设计后、动笔写文档前，AI 必须**主动审查整个方案**，逐项排查潜在问题并给出可执行方案，向用户报告。至少覆盖：

- **IP/子网**：是否存在地址冲突、掩码是否合理、互联网段是否够用、是否出现 31 位/30 位误用；
- **路由**：是否存在黑洞/次优路径/环路、OSPF 区域设计是否合理、是否需下发默认路由、防火墙是否放行 OSPF/BGP；
- **二层**：STP 根桥是否手动指定、是否存在双工/VLAN 透传不一致、Trunk 允许列表是否收敛；
- **高可用**：堆叠/热备是否会有脑裂、VRRP 优先级与抢占是否合理、BFD 检测时长是否匹配；
- **安全**：ACL 方向/通配符是否正确、DMZ 是否暴露过度、管理平面是否隔离、是否存在明文协议；
- **厂商能力**：所填型号是否支持规划特性（例：C2960 为纯二层，不能做三层路由/OSPF；部分特性需授权License）——若型号不支持，必须提示并建议替代；
- **可部署性**：配置命令是否与厂商 CLI 语法一致（参考 `references/*.md`）。

将"问题 → 影响 → 解决方案"逐条列出，与用户确认解决方式。**直至 AI 对整个项目有完全清晰认知、且确认可落地，才进入阶段 6**。

## 阶段 6 · 生成 Word 项目文档

调用 `scripts/gen_docx.py` 生成文档。先构造一个 JSON 项目模型（结构见脚本头部说明），写入 `项目模型.json` 后运行脚本。文档须包含：

1. **项目概览**：背景、目标、三层架构说明；
2. **拓扑图**：用 ASCII 框线/线段抽象描绘（接入↔汇聚↔核心↔防火墙↔出口↔DMZ），参考 `references/topology_examples.md`；
3. **台账表（多张表格）**：设备命名(hostname)、品牌型号、角色、管理 IP、互联接口、归属 VLAN、运行协议、认证账号/密码、备注；
4. **逐模块详述**：用文字解释设计意图与关键点，例如：
   - "ACL 2000 用于出方向 NAT 地址池匹配，仅放行内网网段 10.x/172.x/192.168.x"；
   - "DSW1 的 Loopback0 = 1.1.1.1，作为 OSPF router-id 与远程管理源"；
   - "在核心与汇聚之间为路由优选做了 OSPF cost 调整 / 做了 VRRP 主备切换" 等。

> 安全提示：文档含账号密码等敏感信息，生成后提醒用户妥善保管，避免明文外发。

## 阶段 7 · 生成逐设备配置文件

为**每一台**网络设备生成独立配置文件，命名规则：

```
<主机名>_<角色>_v1.txt
```
- 主机名取设备 `hostname` 配置值（如 `hostname DSW1` → 主机名 `DSW1`）；
- 角色取中文简称：出口设备 / 边界防火墙 / 三层交换机 / 二层交换机 / 服务器 等；
- 示例：`AR1_出口设备_v1.txt`、`FW1_边界防火墙_v1.txt`、`DSW1_三层交换机_v1.txt`、`ASW1_二层交换机_v1.txt`；
- 若配置文件生成后，用户还有要补充的内容且需要修改配置，此时保持原有的配置文件不变，再创建一份新的配置文件，命名示例 `AR1_出口设备_v2.txt`，新文件的版本号v1、v2、v3......以此类推。

**配置命令必须与厂商 CLI 严格一致**，按品牌加载对应参考：

- 思科 → `references/cisco_cmds.md`
- 华为 → `references/huawei_cmds.md`
- 华三 → `references/h3c_cmds.md`

每台文件须包含完整可执行的配置块：主机名、管理 IP/接口、VLAN、Trunk/Access、LACP、OSPF/IS-IS/静态、VRRP、ACL、NAT、AAA、SNMP/Syslog、BFD/NQA、保存命令（`write memory` / `save`）等，并以"逐行可执行"为标准（使用者直接粘贴即可）。

## 输出目录结构

```
网络项目/部署_<项目名>/
├── 项目设计文档.docx        # 阶段6 生成
├── 项目模型.json            # 阶段6 中间产物
├── 配置/
│   ├── AR1_出口设备_v1.txt
│   ├── FW1_边界防火墙_v1.txt
│   ├── DSW1_三层交换机_v1.txt
│   ├── ASW1_二层交换机_v1.txt
│   └── ...
└── 拓扑图.png（可选，若用户需要可视化另绘）
```

## 重要纪律（务必遵守）

- **阶段 3 的关键决策不得自行臆断**；用户未给必须问。
- **非必要细节 AI 可自定**，但必须显式列在草案中供审查，且要等用户"没问题"才锁定。
- **阶段 5 可行性分析是强制关卡**，发现问题必须上报并给方案，不得带病进入文档/配置生成。
- **配置命令务必对照厂商参考文件**，杜绝语法错误导致部署失败。
- 全程用简体中文与用户沟通；文档与配置中的注释可用英文/中文，保持清晰一致。

## 资源索引

- `scripts/gen_docx.py` — Word 文档生成器（读取 JSON 项目模型）。
- `references/workflow.md` — 各阶段细化清单与提问话术模板。
- `references/topology_examples.md` — 三层架构 ASCII 拓扑范例。
- `references/cisco_cmds.md` / `huawei_cmds.md` / `h3c_cmds.md` — 各厂商配置命令速查。
- `references/glossary.md` — ACL、Loopback、VRRP、OSPF cost 等概念的解释话术，供文档详述章节复用。
